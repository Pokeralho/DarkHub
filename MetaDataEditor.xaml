<Page x:Class="DarkHub.MetaDataEditor"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MetaData Editor" Background="{x:Null}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
           Text="MetaData Editor" 
           FontFamily="{StaticResource JetBrainsMono}" 
           FontSize="20" 
           FontWeight="Bold" 
           Foreground="Black" 
           HorizontalAlignment="Center" 
           Margin="0,0,0,10"/>
        
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="btnSelectFile" Content="Selecionar Arquivo" Click="SelectFile_Click" Style="{StaticResource MenuButtonStyle}" Width="150" Height="30"/>
            <Button x:Name="btnSaveMetadata" Content="Salvar Metadados" Click="SaveMetadata_Click" Style="{StaticResource MenuButtonStyle}" Width="150" Height="30" Margin="10,0,0,0"/>
        </StackPanel>
        <ListView x:Name="listViewMetadata" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,0,20,0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Propriedade" Width="200" DisplayMemberBinding="{Binding PropertyName}"/>
                    <GridViewColumn Header="Valor" Width="500">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Width="480"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>