<Page x:Class="DarkHub.ImageTextExtractor"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Image Text Extractor" Background="{x:Null}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="#ECECEC" CornerRadius="5" Padding="5" Margin="0,0,100,5">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="btnSelectImage" Click="SelectImage_Click" Style="{StaticResource MenuButtonStyle}" Width="135" Height="30" ToolTip="Selecionar Imagem">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="/assets/selectimg.png" Width="24" Height="24" Margin="0,0,5,0"/>
                        <TextBlock Text="Selecionar Imagem"/>
                    </StackPanel>
                </Button>
                <Button x:Name="btnExtractText" Click="ExtractText_Click" Style="{StaticResource MenuButtonStyle}" Width="120" Height="30" Margin="10,0,0,0" ToolTip="Extrair Texto">
                    <StackPanel Orientation="Horizontal" Cursor="">
                        <Image Source="/assets/extrairtxtinv.png" Width="24" Height="24" Margin="0,0,10,0"/>
                        <TextBlock Text="Extrair Texto"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <TextBox x:Name="extractedTextBox" Grid.Row="1" IsReadOnly="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" FontFamily="Consolas" FontSize="12" Background="#F5F5F5" BorderBrush="#D3D3D3" BorderThickness="1" Margin="0,0,20,0"/>
    </Grid>
</Page>